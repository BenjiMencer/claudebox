#!/bin/sh
# Stand-in for pip, which is not installed. Without this the failure is
# "No module named pip", which reads like something to work around.
. /usr/local/lib/claudebox/guidance
echo "claudebox: pip is not available in this container." >&2
claudebox_guidance >&2
exit 1
